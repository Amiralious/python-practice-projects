# input_simple_cleaning.py
This practice project:

1. takes the user’s name
2. takes comma-separated skills
3. splits them into a list
4. strips whitespace
5. removes empty items
6. removes duplicates (optionally sorts)
7. prints a clean summary
